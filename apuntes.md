# Indices
Si hacemos un indice a una columna string lo va a pasar a una funcion de hash
## B-Tree
* Numero maximo de elementos que va a tener un nodo: 2*t-1
* La raiz es el unico elemento que podria tener un valor
* Los otros nodos deben tener un minimo t-1 valores.